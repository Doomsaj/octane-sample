{
	{$FRANKENPHP_CONFIG}
	order php_server before file_server
}

{$SERVER_NAME:localhost:8000} {
	root * /var/www/public
	encode zstd br gzip

	php_server {
		worker {
			file /var/www/artisan
			env APP_RUNNING_IN_OCTANE 1
		}
	}
}
